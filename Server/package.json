{
  "name": "server-riot-api",
  "version": "1.0.0",
  "description": "Server write in node and js for live stats /w api riot",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4",
    "request": "^2.88.0",
    "socket.io": "^2.4.0"
  }
}
